#include<stdio.h>
int main(){
//creating variables
int length = 5;
int width = 10;
int area;

//calculating the area of a rectangle
area = length * width;

//print the variables
printf("Length is %d\n", length);
printf("Width is %d\n", width);
printf("Area of the rectangle is %d", area);

return 0;
}